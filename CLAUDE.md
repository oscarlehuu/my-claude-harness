# CLAUDE.md

@AGENTS.md

> This file is a pointer only — the single source of truth is **AGENTS.md** (the line above imports
> it into Claude Code's context). Read & update `AGENTS.md` only; never add content here. Only
> Claude Code reads CLAUDE.md — every other agent/tool reads AGENTS.md.
